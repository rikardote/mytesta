<%= form_for(@adscription) do |f| %>
  <% if @adscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adscription.errors.count, "error") %> prohibited this adscription from being saved:</h2>

      <ul>
      <% @adscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cod %><br>
    <%= f.number_field :cod %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
